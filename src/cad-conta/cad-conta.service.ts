import { Injectable } from '@nestjs/common';
import { cadContaDto } from './cad-conta.dto';

@Injectable()
export class CadContaService {
    cadastrarUsuario(conta: cadContaDto) {
        try {
            
        } catch (error) {
            
        }
    }
}
